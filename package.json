{
  "name": "vaccine-tracker",
  "version": "1.0.0",
  "description": "Slack bot that track available COVID19 vaccine appointments.",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc -p .",
    "build:watch": "tsc -w -p ./src",
    "start": "npm run build && node dist/app.js"
  },
  "license": "MIT",
  "dependencies": {
    "@slack/bolt": "^3.3.0",
    "@types/axios": "^0.14.0",
    "@types/node-schedule": "^1.3.1",
    "axios": "^0.21.1",
    "dotenv": "^8.2.0",
    "node-schedule": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^14.14.35",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.3"
  }
}
